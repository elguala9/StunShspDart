name: stun_shsp
description: A Dart package combining STUN and SHSP protocols.
version: 0.1.0
repository: https://github.com/your-org/stun_shsp_dart

environment:
  sdk: ">=3.5.0 <4.0.0"

resolution: workspace

dependencies:
  stun: ^1.4.0
  singleton_manager: ^0.4.0
  shsp: ^1.5.0
  singleton_manager_generator: ^1.0.4

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
